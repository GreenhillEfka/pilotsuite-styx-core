[pytest]
# Disable pytest-randomly to ensure deterministic test order
# This prevents test isolation issues where tests depend on global state
addopts = -p no:randomly
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

