name: Production Guard (pilotsuite-styx-core)

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  critical-path:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate repo structure
        run: |
          test -d pilot_core && echo OK || echo FAIL
