# PATH AUDIT & FIX REPORT — 2026-03-03 11:20 CET

**Trigger:** HA zeigt Core Add-on 2x + Version-Mismatch (v12.x vs v13.x)  
**Audit Duration:** 20 minutes  
**Auditor:** @clawdya (orchestrated @styx, @cowdya, @groky findings)

---

## 🎯 ROOT CAUSE ANALYSIS: Why HA Shows Add-on 2x

### Primary Issue: Version Drift Between Config Files (RESOLVED)

**Previous Status (from PATH_FIX_MASTER_REPORT_2026-03-03.md):**
- `config.yaml`: version 13.0.3 ✅
- `manifest.json`: version 13.0.0 🔴 MISMATCH
- This caused HA to cache different versions during repository scans

**Current Status (Verified 2026-03-03 11:20):**
```
config.yaml version:           13.0.3 ✅
manifest.json version:         13.0.3 ✅
VERSION (root):                v13.0.3 ✅
VERSION (app):                 13.0.3 ✅
HA manifest.json version:      13.0.3 ✅
```

**Conclusion:** Version sync is now complete. The manifest.json was already fixed in a previous commit.

---

## 📋 PATH INCONSISTENCIES AUDIT

### 1. CRITICAL: Duplicate config.yaml Files

**Check:** Search for ALL config.yaml files in both repos

```bash
# Core repo
$ find /config/.openclaw/workspace/pilotsuite-styx-core -name "config.yaml" -type f
/config/.openclaw/workspace/pilotsuite-styx-core/copilot_core/config.yaml
# ✅ Only ONE config.yaml (correct location)

# HA repo
$ find /config/.openclaw/workspace/pilotsuite-styx-ha -name "config.yaml" -type f
(no output)
# ✅ NO config.yaml files (correct - HA integration doesn't use config.yaml)
```

**Status:** ✅ NO DUPLICATE CONFIG FILES FOUND

---

### 2. CRITICAL: Duplicate manifest.json Files

**Check:** Search for ALL manifest.json files

```bash
# Core repo
$ find /config/.openclaw/workspace/pilotsuite-styx-core -name "manifest.json" -type f
/config/.openclaw/workspace/pilotsuite-styx-core/copilot_core/manifest.json
# ✅ Only ONE manifest.json (correct location)

# HA repo
$ find /config/.openclaw/workspace/pilotsuite-styx-ha -name "manifest.json" -type f
/config/.openclaw/workspace/pilotsuite-styx-ha/custom_components/copilot_ha/manifest.json
# ✅ Only ONE manifest.json (correct location)
```

**Status:** ✅ NO DUPLICATE MANIFEST FILES FOUND

---

### 3. MEDIUM: Dashboard Version Strings

**Check:** Dashboard API version strings

```bash
# HA dashboard/app.py
$ grep -n "version" /config/.openclaw/workspace/pilotsuite-styx-ha/dashboard/app.py | head -5
100:        'version': '13.0.3',
# ✅ Already updated to 13.0.3
```

**Status:** ✅ DASHBOARD VERSION CORRECT

---

### 4. MEDIUM: OpenAPI Documentation Versions

**Check:** OpenAPI spec files

```bash
# openapi.yaml
$ grep "^  version:" /config/.openclaw/workspace/pilotsuite-styx-ha/docs/openapi.yaml
  version: 13.0.3
# ✅ Already updated

# openapi-phase5-6.yaml
$ grep "^  version:" /config/.openclaw/workspace/pilotsuite-styx-ha/docs/openapi-phase5-6.yaml
  version: 13.0.3
# ✅ Already updated
```

**Status:** ✅ OPENAPI VERSIONS CORRECT

---

### 5. LOW: Historical Documentation References

**Files with v12.x references (historical, non-critical):**
- `docs/SWAGGER_UI.md:170` → "Version: 12.5.0" (historical context)
- `docs/SWAGGER_IMPLEMENTATION_COMPLETE.md:154` → "Version: 12.5.0" (historical)
- `docs/API-REFERENCE-PHASE5-6.md:3` → "Version: 12.0.0" (historical)

**Status:** ℹ️ These are historical documentation files, not runtime configs. No action required.

---

## 🔍 WHY HA MIGHT STILL SHOW DUPLICATE (If Issue Persists)

### Possible Causes (Non-Repo Related)

1. **HA Add-on Cache**
   - HA may have cached an old repository scan
   - **Fix:** Restart Home Assistant or refresh add-on store

2. **Multiple Repository Registrations**
   - User may have added the repository twice with different URLs
   - **Check:** HA → Supervisor → Add-on Store → Three-dot menu → Repositories

3. **Legacy Workspace Copies (Not Registered)**
   ```
   /config/.openclaw/agents/styx/agent/pilotsuite-styx-core/ (v7.8.8)
   /config/.openclaw/agents/styx/agent/styx-fork-core/ (v7.8.8)
   /config/.openclaw/workspace-grok-4/pilotsuite-styx-core/ (v8.1.1)
   ```
   - These are NOT registered in HA, but could confuse manual inspection
   - **Recommendation:** Remove if no longer needed

---

## ✅ VERSION SYNC VERIFICATION

### All Version Sources (Current State)

| Component | File | Version | Status |
|-----------|------|---------|--------|
| **Core** | `VERSION` | v13.0.3 | ✅ |
| **Core** | `copilot_core/VERSION` | 13.0.3 | ✅ |
| **Core** | `copilot_core/config.yaml` | 13.0.3 | ✅ |
| **Core** | `copilot_core/manifest.json` | 13.0.3 | ✅ |
| **Core** | `copilot_core/rootfs/.../app/VERSION` | 13.0.3 | ✅ |
| **HA** | `VERSION` | v13.0.3 | ✅ |
| **HA** | `custom_components/copilot_ha/manifest.json` | 13.0.3 | ✅ |
| **HA** | `dashboard/app.py` | 13.0.3 | ✅ |
| **Docs** | `docs/openapi.yaml` | 13.0.3 | ✅ |
| **Docs** | `docs/openapi-phase5-6.yaml` | 13.0.3 | ✅ |

**Result:** ✅ ALL VERSIONS SYNCHRONIZED AT 13.0.3

---

## 📊 GIT STATUS

### pilotsuite-styx-core
```
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
```
**Status:** ✅ Clean, no pending changes

### pilotsuite-styx-ha
```
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
```
**Status:** ✅ Clean, no pending changes

---

## 🎯 CONCLUSION

### Summary of Findings

| Issue Type | Count | Status |
|------------|-------|--------|
| **Critical (Duplicate Configs)** | 0 | ✅ None found |
| **Critical (Version Mismatch)** | 0 | ✅ All synced to 13.0.3 |
| **Medium (Dashboard/API Versions)** | 0 | ✅ All updated |
| **Low (Historical Docs)** | 3 | ℹ️ Informational only |

### Root Cause Determination

**The "2x Add-on" issue is NOT caused by:**
- ❌ Duplicate config.yaml files (verified: only 1 exists)
- ❌ Duplicate manifest.json files (verified: only 1 per repo)
- ❌ Version mismatch (verified: all files show 13.0.3)

**Most Likely Cause:**
- ✅ HA Add-on Store cache showing stale data
- ✅ Possible duplicate repository registration in HA settings

---

## 🔧 RECOMMENDED ACTIONS

### 1. Refresh HA Add-on Store (Primary Fix)

**Option A: UI Method**
1. Open Home Assistant
2. Go to Supervisor → Add-on Store
3. Click three-dot menu (top right)
4. Click "Check for updates"
5. Wait for refresh to complete

**Option B: CLI Method**
```bash
ha supervisor reload
```

### 2. Verify Single Repository Registration

1. Open Home Assistant → Supervisor → Add-on Store
2. Click three-dot menu → "Repositories"
3. Verify only ONE entry for PilotSuite:
   - `https://github.com/GreenhillEfka/pilotsuite-styx-core`
4. If duplicate exists, remove the extra one

### 3. Restart Home Assistant (If Cache Persists)

```bash
ha core restart
```

### 4. Optional: Clean Legacy Workspace Copies

If these directories are no longer needed:
```bash
rm -rf /config/.openclaw/agents/styx/agent/pilotsuite-styx-core/
rm -rf /config/.openclaw/agents/styx/agent/styx-fork-core/
rm -rf /config/.openclaw/workspace-grok-4/pilotsuite-styx-core/
```

---

## 📝 NO CODE CHANGES REQUIRED

**All version files are already synchronized.**

The previous PATH_FIX_MASTER_REPORT_2026-03-03.md identified issues that have since been resolved:
- manifest.json version was already updated to 13.0.3
- Dashboard version strings were already updated to 13.0.3
- OpenAPI docs were already updated to 13.0.3

**This audit confirms:** The repository is in a clean, consistent state.

---

## 🎯 NEXT STEPS

1. **Immediate:** Refresh HA add-on store (see above)
2. **Verify:** Check if duplicate add-on entry disappears
3. **If Issue Persists:** Check HA supervisor logs for add-on scan errors
4. **If Still Unresolved:** Verify no duplicate repository registrations in HA

---

**Audit Completed:** 2026-03-03 11:20 CET  
**Audit Duration:** 20 minutes  
**Files Modified:** 0 (all fixes already applied)  
**Recommended Action:** HA add-on store refresh
